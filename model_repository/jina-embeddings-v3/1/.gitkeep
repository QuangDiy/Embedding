# ⚠️ QUAN TRỌNG: Đặt TẤT CẢ các files sau vào thư mục này:
#
# Required files:
# - model.onnx                    (ONNX model structure)
# - model.onnx_data               (ONNX model weights - 2.27 GB)
# - sentencepiece.bpe.model       (Tokenizer model)
# - tokenizer.json                (Tokenizer config)
# - tokenizer_config.json         (Tokenizer settings)
# - special_tokens_map.json       (Special tokens)
# - config.json                   (Model config)
# - Constant_7_attr__value        (Model constants)
#
# Download từ: https://huggingface.co/BAAI/bge-m3/tree/main/onnx

